name: tipsen
on:
  schedule:
    - cron: '54 1,2 * * *'
    - cron: '44 3 * * *'
    - cron: '14 0,6 * * *'
    
jobs:
  cron:
    runs-on: ubuntu-latest
    steps:
    - name: API req
      run: |
        curl -X GET "https://tipsen.up.railway.app/absen" -o data.json
    - name: API res
      run: cat data.json
